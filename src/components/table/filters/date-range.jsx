import React from "react";
import RangeColumnFilter from "./range";

const DateRangeColumnFilter = ({ column }) => {
  return <RangeColumnFilter column={column} type="date" />;
};

export default DateRangeColumnFilter;
